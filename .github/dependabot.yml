version: 2
updates:
  # Scan Python dependencies for vulnerability alerts and version updates
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
  
  # Scan GitHub Actions workflow dependencies
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
